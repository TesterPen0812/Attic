name: Attic CI

on:
  pull_request:
  push:
    branches:
      - main
      - feature/canvas-ink
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: attic-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  macos:
    name: Build, test, and analyze Attic for macOS
    runs-on: macos-15
    timeout-minutes: 75

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Ruby dependencies
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true

      - name: Generate project artifact
        run: |
          bundle exec ruby Scripts/generate_project.rb \
            --output "$RUNNER_TEMP/GeneratedAttic.xcodeproj"
          ditto -c -k --sequesterRsrc --keepParent \
            "$RUNNER_TEMP/GeneratedAttic.xcodeproj" \
            "$RUNNER_TEMP/GeneratedAttic.xcodeproj.zip"

      - name: Upload generated project
        uses: actions/upload-artifact@v4
        with:
          name: generated-xcode-project-${{ github.sha }}
          path: ${{ runner.temp }}/GeneratedAttic.xcodeproj.zip
          if-no-files-found: error
          retention-days: 7

      - name: Verify generated Xcode project
        id: project_verify
        continue-on-error: true
        run: bundle exec ruby Scripts/verify_project_generation.rb

      - name: Build macOS target
        run: |
          xcodebuild \
            -project "$RUNNER_TEMP/GeneratedAttic.xcodeproj" \
            -target Attic \
            -configuration Local \
            CODE_SIGNING_ALLOWED=NO \
            clean build

      - name: Build with strict concurrency checking
        run: |
          xcodebuild \
            -project "$RUNNER_TEMP/GeneratedAttic.xcodeproj" \
            -scheme Attic \
            -configuration Local \
            -destination 'platform=macOS' \
            -derivedDataPath "$RUNNER_TEMP/AtticStrictBuild" \
            CODE_SIGNING_ALLOWED=NO \
            SWIFT_STRICT_CONCURRENCY=complete \
            clean build

      - name: Run macOS unit tests
        run: |
          xcodebuild \
            -project "$RUNNER_TEMP/GeneratedAttic.xcodeproj" \
            -scheme Attic \
            -configuration Local \
            -destination 'platform=macOS' \
            -derivedDataPath "$RUNNER_TEMP/AtticUnitTests" \
            -resultBundlePath "$RUNNER_TEMP/AtticUnitTests.xcresult" \
            CODE_SIGNING_ALLOWED=NO \
            -only-testing:AtticTests \
            test

      - name: Run macOS UI tests
        run: |
          xcodebuild \
            -project "$RUNNER_TEMP/GeneratedAttic.xcodeproj" \
            -scheme Attic \
            -configuration Local \
            -destination 'platform=macOS' \
            -derivedDataPath "$RUNNER_TEMP/AtticUITests" \
            -resultBundlePath "$RUNNER_TEMP/AtticUITests.xcresult" \
            CODE_SIGNING_ALLOWED=NO \
            -only-testing:AtticUITests \
            test

      - name: Print macOS UI test summary
        if: always()
        run: |
          if [ -d "$RUNNER_TEMP/AtticUITests.xcresult" ]; then
            xcrun xcresulttool get test-results summary \
              --path "$RUNNER_TEMP/AtticUITests.xcresult" \
              --format json || true
          fi

      - name: Upload failing macOS UI test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: macos-ui-test-results-${{ github.sha }}
          path: ${{ runner.temp }}/AtticUITests.xcresult
          if-no-files-found: ignore
          retention-days: 7

      - name: Run macOS static analyzer
        if: always()
        run: |
          xcodebuild \
            -project "$RUNNER_TEMP/GeneratedAttic.xcodeproj" \
            -scheme Attic \
            -configuration Local \
            -destination 'platform=macOS' \
            -derivedDataPath "$RUNNER_TEMP/AtticAnalyze" \
            CODE_SIGNING_ALLOWED=NO \
            analyze

      - name: Require checked-in generated project
        if: always() && steps.project_verify.outcome == 'failure'
        run: |
          echo 'Attic.xcodeproj is not current with Scripts/generate_project.rb'
          exit 1

  iphone:
    name: Build Attic for iPhone Simulator
    runs-on: macos-15
    timeout-minutes: 45

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Ruby dependencies
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true

      - name: Generate iPhone project
        run: |
          bundle exec ruby Scripts/generate_project.rb \
            --output "$RUNNER_TEMP/GeneratedAttic.xcodeproj"

      - name: Verify generated Xcode project
        id: project_verify
        continue-on-error: true
        run: bundle exec ruby Scripts/verify_project_generation.rb

      - name: Select an available iPhone simulator
        shell: python
        run: |
          import json
          import os
          import subprocess

          payload = json.loads(
              subprocess.check_output(
                  ["xcrun", "simctl", "list", "devices", "available", "-j"],
                  text=True,
              )
          )
          for runtime in sorted(payload["devices"], reverse=True):
              for device in payload["devices"][runtime]:
                  if device["name"].startswith("iPhone") and device.get("isAvailable", True):
                      with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as output:
                          output.write(f"IOS_SIMULATOR_UDID={device['udid']}\n")
                      raise SystemExit(0)
          raise SystemExit("No available iPhone simulator was found")

      - name: Build iPhone app and tests
        run: |
          xcodebuild \
            -project "$RUNNER_TEMP/GeneratedAttic.xcodeproj" \
            -scheme AtticMobile \
            -configuration Local \
            -destination "platform=iOS Simulator,id=$IOS_SIMULATOR_UDID" \
            -derivedDataPath "$RUNNER_TEMP/AtticMobileBuild" \
            CODE_SIGNING_ALLOWED=NO \
            SWIFT_STRICT_CONCURRENCY=complete \
            clean build-for-testing

      - name: Require checked-in generated project
        if: always() && steps.project_verify.outcome == 'failure'
        run: |
          echo 'Attic.xcodeproj is not current with Scripts/generate_project.rb'
          exit 1
