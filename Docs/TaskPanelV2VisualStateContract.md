# Visual state and performance acceptance — user update 2026-09-13

Applies alongside the full 15-section QualityChecklist, not a reduced replacement. Every implemented visible state needs a reproducible trigger, expected behavior, actual screenshot/native interaction evidence, exact preview identity, and Pass/Fail/Partial. Inventory states from UI branches as well as the checklist: empty, populated, long/overflow, hover, keyboard focus, editing, disabled, importing/loading, pending, success, errors, cancellation/retry, confirmation, dragging, accepted/rejected drop, scrolling, pin/unpin, moved panels, view transitions, relevant appearance/accessibility settings. Cover meaningful combinations and lifecycle boundaries; do not claim exhaustive combinatorial proof.

Errors must actually be triggered using isolated test fixtures or safe test-only failure injection where deterministic reproduction is otherwise unavailable. Inspect wording, length, containment, contrast, layout, recovery, draft preservation and repeated failures. Do not reset stores, damage real files or change production permissions to generate errors.

User screenshot Image-43E6B1EC-695E-4407-B331-C883583467C7.png: attachment error expands composer into an oversized pill; long filename/raw size text clips. Required repair: concise actionable visible message, bounded layout and accessible full details, stable composer and recovery; verify large-file rejection and other implemented error branches.

User screenshot Image-C28F9A86-532E-4FE2-80DD-BADB7349F0FE.png: attachment drag preview is a bulky rounded blob with filename wrapped into three lines. Required repair: compact legible image/file drag representation in Attic's visual language. Check native drag-out, drag-in, panel-to-panel transfer, cancellation, rejected targets, long filenames and multiple items where supported.

User explicitly reports lag during attachment drag-out and drag-in. Measure initiation delay, tracking, target feedback, import/drop completion, scrolling and return to idle with representative bounded fixtures; assess active CPU/memory and repeated interaction growth. Idle CPU and static screenshots do not prove smoothness. Retain all features; prefer measured causal fixes over broad speculative refactors.

Pointer interruption clarification: user confirms they moved the pointer during prior checks. Do not blame another worker or classify interrupted runs as product failures without controlled reproduction. Do not fight user input. Stop injecting during interference, reacquire fresh window/AX geometry and resume/repeat the affected scenario when clear. Continue the audit, record interrupted trials separately, and do not mark them Pass. Only one agent drives UI at once.

Workflow update: Opus5 HIGH implements; SWE code and SWE visual independently review completed batches; fixes return to Opus and affected scenarios are retested. Final code reviewer Sol HIGH. User latest message refers to Astra LIGHT visual review; assess final visual effort explicitly rather than silently retaining older Astra HIGH-only routing. After functional verification, requested Fable5.1 MEDIUM optimization session requires live provider/model availability verification, measured scope and its own verification; follow with Astra LIGHT visual regression pass. Recommendation pending user discussion: put measured optimization before final acceptance to avoid repeating comprehensive review.
