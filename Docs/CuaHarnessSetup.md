# Cua Driver harness setup — 2026-09-13

User requested proper computer-use tools for Opus/Synara. Installed official stable Cua Driver0.28.1 from cua.ai/driver/install.sh with --no-modify-path. App /Applications/CuaDriver.app, executable /Users/taha/.local/bin/cua-driver. Added user-scoped Claude MCP cua-computer-use using generated mcp-config; claude mcp get reports Connected. Existing registrations preserved. Synara installed apps/server/dist/index.mjs sets settingSources from CLAUDE_SETTING_SOURCES in its Claude Agent SDK launch; user settings included in related initialization. Need actual new Opus session discovery validation, not claimed performed. No Attic implementation work resumed.

Daemon started via LaunchServices, standard mode, no bypass flags. status reports running PID44842, socket ~/Library/Caches/cua-driver/cua-driver.sock, authorization host unavailable; permissions status reports unknown and read-only check_permissions returns permissions_pending. Therefore desktop observation/input not yet verified. Await explicit confirmation before enabling Accessibility/Screen Recording grants. Do not call it working computer-use access yet. Installer telemetry remains default enabled; no shell rc edits. No autostart installed.

Next: user-approved macOS permission setup, confirm live capture, verify Opus sees MCP image/tool results in Synara, then bounded Attic interaction smoke. User can explicitly authorize the needed registration and TCC actions. Foreground/physical trackpad/drag performance still need actual validation. Reference https://cua.ai/docs/how-to-guides/driver/connect-your-agent and https://cua.ai/docs/how-to-guides/driver/use-claude-agent-sdk .

## Correction and live setup — user clarified SWE target
User explicitly approved Accessibility and Screen Recording and clarified this is for SWE visual reviewers. Native settings showed CuaDriver Screen Recording ON; permission grant command completed with both grants + direct capture verified under driver-daemon identity at2026-09-13T15:44:43Z. Registered Devin user MCP via `devin mcp add --scope user cua-driver -- /Users/taha/.local/bin/cua-driver mcp`; get confirms command. Earlier Claude registration remains available, not a substitute for Devin verification.
Bounded SWE harness test task agent-35448e72bd885a868fb6260297ba2f5d, request attic-swe-cua-harness-smoke-20260913-01. Owns Docs/CuaSWEHarnessSmoke.md; actual MCP image/navigation proof required. No full audit/implementation resumed; heartbeat stays paused.

## Devin compatibility repair
Initial SWE test proved only MCP text summary reached model; `list_windows` raw JSON-RPC contains structuredContent windows while content contains only "Found2windows". Added /Users/taha/.local/share/attic-cua/mcp-compat.py: transparent stdio adapter retains original JSON-RPC and image blocks, appends structuredContent as text. Devin cua-driver entry only changed command to /usr/bin/python3 and args to adapter path; original config backed up privately in same directory, no other entries changed. Pure fixtures verify image/error/notification preservation; direct live MCP check proves window_id present in text. No additional permissions. This is local compatibility code, not official Cua functionality.
Original SWE smoke stopped at its time limit, no navigation pass. Fresh SWE task agent-651ed4702adb7074ff586e3a5fb37488 uses updated config, explicit real desktop cursor route and4-minute bound, report Docs/CuaSWECompatSmoke.md. Await actual SWE data/image/navigation verdict before calling ready.

Final setup correction: Claude cua-computer-use registration removed; Devin cua-driver compatibility registration retained. See CuaSWECompatSmoke.md for actual SWE screenshot/navigation proof and remaining limitations. Both test workers stopped; UI lock absent.
