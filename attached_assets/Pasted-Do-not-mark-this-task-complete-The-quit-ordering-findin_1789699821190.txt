Do not mark this task complete. The quit-ordering finding and patch are useful; preserve them. The original comprehensive whole-app audit is still incomplete: this run read only 2,689 lines and covered essentially one cross-subsystem path. Continue the same task now using Claude Fable 5.1 in Max mode at maximum effort.

Complete the coverage matrix and independently inspect every production subsystem and major user journey: Tasks and Backlog plus replica-safe mutations; Notes, drafts, and attachment persistence; Canvas store, session, import, render, history, and semantic editing; task and note file storage and cleanup; AppCoordinator startup and shutdown; AgentServer, MCP, security, and hotkey; panel, window, corner, and swipe state machines; Settings, themes, and accessibility; persistence, recovery, and corruption; concurrency and cancellation; and structural performance and resource risks. Trace callers and existing tests, reconcile findings against current source and prior audits, and explicitly record areas examined, hypotheses disproved, confirmed findings, and remaining native-only gaps.

For each finding include severity, confidence, exact paths, trigger, causal trace, user impact, existing defenses and tests, minimal remedy, and Mac validation plan. Do not inflate scope with cosmetic refactors. Implement only additional high-confidence, narrow fixes that have meaningful regression tests, and preserve the current quit-ordering patch. If a finding cannot be safely fixed without macOS verification, document it rather than guessing. Review every hunk; keep work on the existing dedicated branch; do not delegate to Astra; do not push, merge, deploy, access user stores or secrets, activate iPhone, CloudKit, or APNs, buy credits, or enable overages.

Do not stop after the first additional finding. Continue until the coverage matrix is complete and the checked-in report demonstrates repository-wide coverage. State exact files and lines reviewed, actual final branch and SHA, checks run, unrun macOS gates, and ordered handoff. Remain within the existing credits and stop before any extra paid usage.